[develop]
script_dir=$base/lib/proc_planner_ros2
[install]
install_scripts=$base/lib/proc_planner_ros2
[tool:pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial
    pylint
